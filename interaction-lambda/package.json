{
  "name": "interaction-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "compile": "tsc",
    "postcompile": "cp package*.json build && cd build && npm install --only=production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.83",
    "@types/express": "^4.17.13",
    "@types/node": "^16.10.2",
    "aws-sdk": "2.997.0",
    "typescript": "^4.4.3"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord-interactions": "^2.3.0"
  }
}
